# --- Inference pipeline ---
torch
transformers>=4.57   # Qwen3-VL (judge + several backends) requires >=4.57
pillow
numpy
pandas
pyyaml
tqdm

# --- Evaluation pipeline ---
# Local judge backend (default): runs Unison-Judge across GPUs.
accelerate
openai

